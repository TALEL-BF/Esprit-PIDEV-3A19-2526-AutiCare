<div id="sidebar">
    <div class="sidebar-brand">
        <div class="brand-icon"><i class="fas fa-brain"></i></div>
        <div class="brand-text">
            <h1>AutiCare</h1>
            <span>Mon Espace</span>
        </div>
    </div>

    <nav class="sidebar-nav">
        <div class="nav-section-label">NAVIGATION</div>
        <a class="nav-link-item {% if app.request.get('_route') == 'enfant_dashboard' %}active{% endif %}" 
           href="{{ path('enfant_dashboard') }}">
            <i class="fas fa-home"></i> <span>Accueil</span>
        </a>
        
        <div class="nav-section-label">ACTIVITÉS</div>
        
        <a href="#" class="nav-link-item">
            <i class="fas fa-book-open"></i> <span>Mes cours</span>
        </a>
        <a href="#" class="nav-link-item">
            <i class="fas fa-gamepad"></i> <span>Mes jeux</span>
        </a>
        <a href="#" class="nav-link-item">
            <i class="fas fa-calendar-week"></i> <span>Mon emploi du temps</span>
        </a>
        <a href="#" class="nav-link-item">
            <i class="fas fa-calendar-alt"></i> <span>Événements</span>
        </a>
        <a href="#" class="nav-link-item">
            <i class="fas fa-trophy"></i> <span>Mes récompenses</span>
        </a>
        
        <!-- Mon Profil -->
        <a href="{{ path('app_profile') }}" class="nav-link-item">
            <i class="fas fa-user-circle"></i> <span>Mon Profil</span>
        </a>
        
        <div class="nav-section-label">COMPTE</div>
        <a href="{{ path('app_logout') }}" class="nav-link-item">
            <i class="fas fa-sign-out-alt"></i> <span>Déconnexion</span>
        </a>
    </nav>

    <div class="sidebar-footer">
        <div class="admin-info">
            <div class="admin-avatar">{{ app.user.prenom|first|upper }}</div>
            <div>
                <div class="admin-name">{{ app.user.prenom }} {{ app.user.nom }}</div>
                <div class="admin-role">Élève</div>
            </div>
        </div>
    </div>
</div>

<script>
function toggleSection(event) {
    if (event) event.stopPropagation();
    const parent = event.target.closest('.nav-item-wrap');
    if (parent) parent.classList.toggle('open');
}
</script>