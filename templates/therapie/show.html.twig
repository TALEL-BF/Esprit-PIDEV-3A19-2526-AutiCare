{% extends 'base.html.twig' %}

{% block title %}TherapieEntity{% endblock %}

{% block body %}
    <h1>TherapieEntity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ therapie_entity.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_therapie_index') }}">back to list</a>

    <a href="{{ path('app_therapie_edit', {'id': therapie_entity.id}) }}">edit</a>

    {{ include('therapie/_delete_form.html.twig') }}
{% endblock %}
