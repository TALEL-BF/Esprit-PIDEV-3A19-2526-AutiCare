<form method="post" action="{{ path('app_therapie_delete', {'id': therapie_entity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ therapie_entity.id) }}">
    <button class="btn">Delete</button>
</form>
