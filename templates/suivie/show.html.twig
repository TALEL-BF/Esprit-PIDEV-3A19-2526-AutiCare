{% extends 'base.html.twig' %}

{% block title %}SuivieEntity{% endblock %}

{% block body %}
    <h1>SuivieEntity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ suivie_entity.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_suivie_index') }}">back to list</a>

    <a href="{{ path('app_suivie_edit', {'id': suivie_entity.id}) }}">edit</a>

    {{ include('suivie/_delete_form.html.twig') }}
{% endblock %}
