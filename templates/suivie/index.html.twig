{% extends 'base.html.twig' %}

{% block title %}SuivieEntity index{% endblock %}

{% block body %}
    <h1>SuivieEntity index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for suivie_entity in suivie_entities %}
            <tr>
                <td>{{ suivie_entity.id }}</td>
                <td>
                    <a href="{{ path('app_suivie_show', {'id': suivie_entity.id}) }}">show</a>
                    <a href="{{ path('app_suivie_edit', {'id': suivie_entity.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_suivie_new') }}">Create new</a>
{% endblock %}
