{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

<div class="row g-3">
    <div class="col-md-4">
        {{ form_label(form.enfantId, 'Id enfant', {'label_attr': {'class': 'form-label fw-semibold'}}) }}
        {{ form_widget(form.enfantId, {'attr': {'class': 'form-control', 'placeholder': 'Ex: 1'}}) }}
        <div class="text-danger small mt-1">{{ form_errors(form.enfantId) }}</div>
    </div>

    <div class="col-md-4">
        {{ form_label(form.noteCours, 'Note de cours', {'label_attr': {'class': 'form-label fw-semibold'}}) }}
        {{ form_widget(form.noteCours, {'attr': {'class': 'form-control', 'placeholder': 'Ex: 12.50'}}) }}
        <div class="text-danger small mt-1">{{ form_errors(form.noteCours) }}</div>
    </div>

    <div class="col-md-4">
        {{ form_label(form.noteConsultation, 'Note de consultation', {'label_attr': {'class': 'form-label fw-semibold'}}) }}
        {{ form_widget(form.noteConsultation, {'attr': {'class': 'form-control', 'placeholder': 'Ex: 14.00'}}) }}
        <div class="text-danger small mt-1">{{ form_errors(form.noteConsultation) }}</div>
    </div>

    <div class="col-md-6">
        {{ form_label(form.moyenne, 'Moyenne générale', {'label_attr': {'class': 'form-label fw-semibold'}}) }}
        {{ form_widget(form.moyenne, {'attr': {'class': 'form-control'}}) }}
        <small class="text-muted">Cette moyenne sera utilisée pour attribuer le niveau.</small>
        <div class="text-danger small mt-1">{{ form_errors(form.moyenne) }}</div>
    </div>

    <div class="col-md-6">
        <label class="form-label fw-semibold">Niveau calculé</label>
        <input type="text" class="form-control" value="Attribué automatiquement selon la moyenne générale" disabled>
        <small class="text-muted">Le niveau sera calculé automatiquement.</small>
    </div>

    <div class="col-12">
        {{ form_label(form.remarque, 'Remarque', {'label_attr': {'class': 'form-label fw-semibold'}}) }}
        {{ form_widget(form.remarque, {'attr': {'class': 'form-control', 'rows': 3, 'placeholder': 'Remarque facultative...'}}) }}
        <div class="text-danger small mt-1">{{ form_errors(form.remarque) }}</div>
    </div>
</div>

<div class="d-flex gap-2 mt-4">
    <button class="btn-purple" type="submit">
        <i class="fas fa-save"></i>
    </button>

    <a href="{{ path('app_suivi_total_index') }}" class="btn-outline-purple text-decoration-none">
        <i class="fas fa-arrow-left"></i>
    </a>
</div>

{{ form_end(form) }}