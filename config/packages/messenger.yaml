# config/packages/messenger.yaml
framework:
    messenger:
        default_bus: messenger.bus.default

        buses:
            messenger.bus.default: ~

        transports:
            # vide → pas d'async
            sync: 'sync://'

        routing:
            # 🔴 IMPORTANT: on NE met PAS SendEmailMessage ici